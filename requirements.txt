# == 文字嵌入與貼文生成 ==
sentence-transformers==3.4.1     # 需要 torch，但我們自己 pin
transformers>=4.41.0,<4.42
huggingface-hub>=0.23.0
torch==2.7.0                     # 拿掉 +cpu，PyPI 直接有

# == Web / API ==
requests>=2.31.0
undetected-chromedriver>=3.5.5

# == 數據處理 ==
numpy>=1.26.4
sqlite3>=0.0.0; python_version<"3.11"  # 內建保險條

# == 其他 ==
# 沒了，保持精簡，減少 CI 下載時間
