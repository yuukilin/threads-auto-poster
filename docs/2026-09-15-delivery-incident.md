# 2026-09-15 草稿交付停滯排查

## 查證結果（台北時間）

- 9 月 11 日 12:30 的工作因舊 prompt 綁死 Markdown 標題層級而停止；當天下午已修正並交付棕櫚油草稿。
- 9 月 14 日 12:33 啟動，13:21 完成糖草稿；9 月 15 日 12:31 啟動，13:07 完成咖啡草稿。原始工作紀錄及草稿檔均存在。
- 9 月 14 日 15:59 起查到對話歷史投影錯誤：`expected ordinal 1824, got 1823`。第一次回查只拿到 9 月 2 日舊進度；第一處修正後又遇到 `expected ordinal 3548, got 3547`。
- 兩處都來自 `token_count` 與下一筆 `thread_settings_applied` 使用相同序號。這使新版歷史索引無法讀入後續完成紀錄，造成狀態與原始工作結果不同步。
- 權杖在 9 月 14 日 15:33 正常續期，LaunchAgent 結束碼為 0；9 月 11 日的 stderr 為舊紀錄。

## 已執行修復

完整備份本機對話紀錄，保留全部使用者、助理、工具訊息的原始位元與序號，只移除兩筆重複編號的 `token_count` 系統計數紀錄，並調整該對話的歷史讀取位置。備份和操作收據僅存於 `.local/history-repair-backups/`，不進 Git。這次操作沒有改動權杖、帳號授權或其他對話。

修復後，對話歷史可回查 9 月 14、15 日完成的草稿及本次進行中的修復對話。`codex_thread_store::local::live_writer` 在修復後沒有再產生 WARN／ERROR。原始紀錄序號已連續。

## 防止再次靜默漏稿

- `draft_workflow.py` 以台北日期建立獨立批次，只檢查當日草稿。
- 完整正文必須出現在同對話、已完成的最終回覆，才算交付；寫好檔案、短通知、進行中的回覆均不算。
- 當日有檔案但沒有交付證據，就交付原稿；舊日待核准稿不會阻擋新工作日。
- 原 heartbeat 保留同一對話，12:30 出稿，13:30、14:30 續查並補跑。已交付時安靜結束。
- 文字目標為啟動後 15 分鐘內交付，圖表搜尋最多 3 分鐘。每日檢查不讀鑰匙圈。
- `.local/draft-runs/YYYY-MM-DD.json` 保存實際驗證到的回覆 ID、版本及文字雜湊；在 final 之前不預先聲稱交付成功。

## 驗證

- 24 項單元測試通過，包含未交付原稿恢復、完整 final 判斷、修改版再次交付、昨日待核准不擋新稿、舊日報待補跑及權杖異常不擋草稿。
- 實際回查 9 月 15 日 358 字咖啡草稿，全文匹配 13:07:15 的 completed final。
- 以 9 月 16 日預檢，回傳等待 9 月 16 日報，不會挪用 9 月 15 日草稿。
- 已從排程設定與實際排程資料庫讀回 ACTIVE、原對話 ID、12:30／13:30／14:30。

排程需要這台 Mac 開機且 Codex App 正在執行。尚未發生的未來執行，應以當日交付收據驗證，而非把今天的測試當成明天的完成紀錄。
